# Base URL of the EcoTrack backend. Defaults to http://localhost:8080 if unset.
# Copy this file to ".env" to override (e.g. when the backend runs on another host).
VITE_API_URL=http://localhost:8080
